'use strict';

/**
 * @ngdoc function
 * @name openbusApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the openbusApp
 */
angular.module('openbusApp')
  .controller('MainCtrl', function ($rootScope) {
    $rootScope.pageTitle = "home";

  });
