'use strict';

describe('Service: service/request/index', function () {

  // load the service's module
  beforeEach(module('openbusApp'));

  // instantiate service
  var service/request/index;
  beforeEach(inject(function (_service/request/index_) {
    service/request/index = _service/request/index_;
  }));

  it('should do something', function () {
    expect(!!service/request/index).toBe(true);
  });

});
