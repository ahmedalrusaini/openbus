'use strict';

describe('Service: tables/tableFilters', function () {

  // load the service's module
  beforeEach(module('openbusApp'));

  // instantiate service
  var tables/tableFilters;
  beforeEach(inject(function (_tables/tableFilters_) {
    tables/tableFilters = _tables/tableFilters_;
  }));

  it('should do something', function () {
    expect(!!tables/tableFilters).toBe(true);
  });

});
